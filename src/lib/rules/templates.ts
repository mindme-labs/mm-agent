export function fillTemplate(template: string, vars: Record<string, string>): string {
  return template.replace(/\{(\w+)\}/g, (_, key) => vars[key] ?? `{${key}}`)
}

export function formatAmount(amount: number): string {
  return amount.toLocaleString('ru-RU', { maximumFractionDigits: 2 })
}

export function formatAmountShort(amount: number): string {
  if (amount >= 1_000_000) return `${(amount / 1_000_000).toFixed(1)} млн ₽`
  if (amount >= 1_000) return `${(amount / 1_000).toFixed(0)} тыс. ₽`
  return `${amount.toFixed(0)} ₽`
}

export const TEMPLATES = {
  'ДЗ-1а': `Тема: Сверка расчётов по договору поставки

Уважаемые коллеги,

Просим провести сверку расчётов по нашему договору поставки.
По нашим данным, за {counterparty} числится задолженность в размере
{amount} руб. Последняя оплата поступала более {months} мес. назад.

Просим подтвердить сумму и при наличии расхождений направить нам
ваш вариант акта сверки.

С уважением,
{companyName}`,

  'ДЗ-1б': `ДОСУДЕБНАЯ ПРЕТЕНЗИЯ

{counterparty}

Настоящим уведомляем о просроченной задолженности по договору поставки.

Сумма основного долга: {amount} руб.
Период просрочки: более {months} мес.
Расчёт пени (0.1% в день): {penaltyAmount} руб.
Итого к оплате: {totalAmount} руб.

Просим в срок до {deadline} произвести оплату в полном объёме.
В противном случае мы будем вынуждены обратиться в арбитражный суд.

С уважением,
{companyName}`,

  'ДЗ-2': `Тема: Специальное предложение по расчётам

Уважаемые коллеги, за вами числится задолженность {amount} руб.
Предлагаем скидку {discount}% при оплате {partialAmount} руб. до {deadline}.
Сумма со скидкой: {discountedAmount} руб. Предложение на 3 рабочих дня.

С уважением, {companyName}`,

  'ДЗ-3': `{contactName}, добрый день!

Мы заметили, что давно не получали от вас заказов. Обычно вы закупаете
примерно раз в {usualCycle}. Хотим уточнить — всё ли в порядке?
Готовы предложить специальные условия. Среднемесячный объём ваших закупок
составлял {avgMonthlyAmount} руб.

С уважением, {managerName}, {companyName}`,

  'КЗ-1': `Тема: Статус отгрузки по оплаченному счёту

Уважаемые коллеги, нами оплачен аванс {amount} руб. в адрес {counterparty}.
Поступление товара/услуг на данную сумму не зафиксировано.
Просим уточнить статус отгрузки.

С уважением, {companyName}`,

  'ЗАП-1': `Добрый день! У нас на складе — "{productName}", остаток на
{amount} руб. Не реализовывался более 90 дней. Скидка 15%.
Предложение на 7 дней.`,

  'ЗАП-2': `Служебная записка
Кому: Менеджер по закупкам
Тема: Затоваривание — "{productName}"

Остаток: на {amount} руб.
Ср. продажи: {avgMonthlySales} руб./мес.
Обеспеченность: {stockDays} дней (норма: 90 дней).
Рекомендация: приостановить закупку на квартал.`,

  'ФЦ-1': `Стратегическая рекомендация: Оптимизация платёжного цикла

Оборачиваемость ДЗ: {dzDays} дн. (столько клиенты платят вам)
Оборачиваемость КЗ: {kzDays} дн. (столько вы платите поставщикам)
Разрыв: {delta} дн. Замороженный капитал: ~{frozenAmount} руб.

Рекомендации: сократить типовую отсрочку для новых клиентов,
провести переговоры с поставщиками об увеличении отсрочки.`,
}
